// Placeholder screen — không được render trực tiếp.
// Tab "Thêm" sử dụng tabBarButton tùy chỉnh trong _layout.tsx
import { View } from 'react-native';
export default function MoreButtonPlaceholder() {
  return <View />;
}
